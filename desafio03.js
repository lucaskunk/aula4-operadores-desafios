libra = Number(prompt("Digite um valor em libra para ser convertido em Kg: "))

libraParaKg = libra / 2.205

console.log(libra, "lb equivalem a", libraParaKg, "Kg")

onça = Number(prompt("Digite um valor em onça para ser convertido em Kg"))

onçaParaKg = onça / 35.274

console.log(onça, "oz equivalem a ", onçaParaKg, "Kg")

milhas = Number(prompt("Digite uma distância em milhas para ser convertida em metros: "))

milhasToMetros = milhas * 1609

console.log(milhas, "mi equivalem a ", milhasToMetros, "m.")

pes = Number(prompt("Digite uma distância em pés para ser convertida em metros: "))

pesToMetros = pes / 3.281

console.log(pes, "ft equivalem a ", pesToMetros, "m.")

galao = Number(prompt("Digite um peso em galão para ser convertido em litros: "))

galaoToLitros = galao * 3.785

console.log(galao, "gal equivalem a ", galaoToLitros, "l")

xicara = Number(prompt("Digite um peso em xícaras para ser convertido em litros: "))

xicaraToLitros = xicara * 0.24

console.log(xicara, " xic equivalem a ", xicaraToLitros, "l")